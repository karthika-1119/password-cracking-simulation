# password-cracking-simulation
This project demonstrates how weak passwords can be cracked using dictionary attacks. It simulates a real-world authentication system where passwords are stored as SHA-256 hashes.
